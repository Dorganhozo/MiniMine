name: CI - Compilacao MiniMine

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Compilacao Multiplataforma
    runs-on: ubuntu-latest

    steps:
      - name: Checagem do código
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle 8.7
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.7'

      - name: Dar permissão ao Gradlew
        run: chmod +x gradlew

      - name: Compilação PC
        run: ./gradlew gdx-pc:rodar

      - name: Compilação Android
        run: ./gradlew gdx-android:assembleDebug

      - name: name: CI - Compilacaoo MiniMine

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Compilacao Multiplataforma
    runs-on: ubuntu-latest

    steps:
      - name: Checagem do código
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle 8.7
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.7'

      - name: Dar permissão ao Gradlew
        run: chmod +x gradlew

      - name: Compilacao PC
        run: ./gradlew gdx-pc:rodar

      - name: Compilacao Android
        run: ./gradlew gdx-android:assembleDebug

      - name: PC artefato
        uses: actions/upload-artifact@v4
        with:
          name: MiniMine-PCS
          path: gdx-pc/build/libs/*.jar

      - name: Android APK
        uses: actions/upload-artifact@v4
        with:
          name: MiniMine-Android
          path: gdx-android/build/outputs/apk/debug/*.apk
        uses: actions/upload-artifact@v4
        with:
          name: MiniMine-PCS
          path: gdx-pc/build/libs/*.jar

      - name: Android APK
        uses: actions/upload-artifact@v4
        with:
          name: MiniMine-Android
          path: gdx-android/build/outputs/apk/debug/*.apk
